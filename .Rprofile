# Example .Rprofile for initializing R in Codespaces
options(repos = c(CRAN = "https://cloud.r-project.org")) # Set default CRAN mirror
library(tidyverse) # Load frequently used packages automatically
